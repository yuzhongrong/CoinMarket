<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="vm"
            type="com.zksg.kudoud.state.MainActivityViewModel" />

<!--        <variable-->
<!--            name="click"-->
<!--            type="" />-->

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:windowSoftInputMode="adjustPan"
        android:layout_height="match_parent"
        android:background="@drawable/bg_main"
        android:fitsSystemWindows="true"
        android:orientation="vertical"

        >

        <FrameLayout
            android:id="@+id/main_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_above="@+id/nav_root"

            />


        <LinearLayout
            android:id="@+id/nav_root"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="@dimen/dp16"
            app:shape_solidColor="@color/c_1f1f1f"
            app:shape_radius="@dimen/dp24"
            android:layout_alignParentBottom="true"
            >

            <com.zksg.kudoud.widgets.NavigateTabBar
                android:id="@+id/home_navigate"
                android:layout_width="match_parent"
                android:layout_height="@dimen/dp48"
                app:containerId="@+id/main_container"
                android:layout_marginBottom="@dimen/dp15"
                />

        </LinearLayout>


    </LinearLayout>

</layout>